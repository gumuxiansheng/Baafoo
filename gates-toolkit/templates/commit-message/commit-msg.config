# commit-msg 校验配置（由 gates-toolkit 生成，可手动编辑）
# 修改后无需重跑 setup：commit-msg hook 每次运行都会读取本文件
# 格式：key="value"，勿改 key 名
TYPES="feat fix docs style refactor perf test build ci chore revert"
SUBJECT_MAX="50"
REQUIRED_SECTIONS="改动说明 测试情况 影响范围"
