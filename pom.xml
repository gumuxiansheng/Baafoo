<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.baafoo</groupId>
    <artifactId>baafoo-parent</artifactId>
    <version>1.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Baafoo Parent</name>
    <description>Baafoo - JavaAgent Based API Mock Platform</description>

    <modules>
        <module>baafoo-core</module>
        <module>baafoo-plugin-api</module>
        <module>baafoo-agent</module>
        <module>baafoo-server</module>
        <module>baafoo-cli</module>
        <module>baafoo-test-app</module>
        <module>baafoo-test-spring</module>
        <module>baafoo-example-plugins/tdmq</module>
        <module>baafoo-test-pulsar</module>
        <module>baafoo-testcontainers</module>
        <module>baafoo-spring-boot-starter-test</module>
        <module>baafoo-coverage</module>
    </modules>

    <properties>
        <java.version>1.8</java.version>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Core -->
        <!-- Jackson 2.18.8 (LTS, Java 8 compatible): fixes CVE-2025-52999 (core async-parser DoS),
             CVE-2026-59888 (@JsonIgnore bypass), CVE-2026-54512 (PolymorphicTypeValidator bypass, 8.1). -->
        <jackson.version>2.18.8</jackson.version>
        <snakeyaml.version>2.2</snakeyaml.version>

        <!-- Agent -->
        <byte-buddy.version>1.14.14</byte-buddy.version>

        <!-- Server -->
        <netty.version>4.1.136.Final</netty.version>
        <artemis.version>2.19.1</artemis.version>
        <h2.version>2.2.224</h2.version>
        <hikaricp.version>4.0.3</hikaricp.version>
        <postgresql.version>42.7.5</postgresql.version>
        <mybatis.version>3.5.15</mybatis.version>

        <!-- Logging -->
        <slf4j.version>1.7.36</slf4j.version>
        <logback.version>1.3.15</logback.version>

        <!-- JWT -->
        <jjwt.version>0.11.5</jjwt.version>

        <!-- Password Hashing -->
        <jbcrypt.version>0.4</jbcrypt.version>

        <!-- Spring Boot -->
        <spring-boot.version>2.7.18</spring-boot.version>

        <!-- Security overrides (SCA remediation, all Java 8 compatible) -->
        <!-- Guava: CVE-2023-2976 fixed in 32.0.1; -jre flavor supports Java 8 -->
        <guava.version>33.6.0-jre</guava.version>
        <!-- Commons Text: CVE-2022-42889 (Text4Shell) fixed in 1.10.0 -->
        <commons-text.version>1.12.0</commons-text.version>
        <!-- snappy-java (test scope): CVE-2023-43642 fixed in 1.1.10.3 -->
        <snappy-java.version>1.1.10.4</snappy-java.version>
        <!-- Spring / Tomcat (test-spring only): patch within Java 8 lines -->
        <spring.version>5.3.39</spring.version>
        <tomcat.version>9.0.102</tomcat.version>

        <!-- Test -->
        <junit.version>4.13.2</junit.version>
        <mockito.version>4.11.0</mockito.version>
        <testcontainers.version>1.19.8</testcontainers.version>

        <!-- Frontend build (set -Dnpm.skip=true in CI without Node.js) -->
        <npm.skip>false</npm.skip>

        <!-- Plugin -->
        <maven-shade-plugin.version>3.5.0</maven-shade-plugin.version>
        <maven-jar-plugin.version>3.3.0</maven-jar-plugin.version>
        <maven-compiler-plugin.version>3.11.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>2.22.2</maven-surefire-plugin.version>
        <jacoco.version>0.8.11</jacoco.version>
        <pitest.version>1.15.0</pitest.version>
        <pitest.mutationThreshold>0</pitest.mutationThreshold>
        <pitest.coverageThreshold>0</pitest.coverageThreshold>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>com.baafoo</groupId>
                <artifactId>baafoo-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baafoo</groupId>
                <artifactId>baafoo-plugin-api</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baafoo</groupId>
                <artifactId>baafoo-testcontainers</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baafoo</groupId>
                <artifactId>baafoo-spring-boot-starter-test</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- JSON -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-yaml</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <!-- JSR310: Java 8 date/time support (OffsetDateTime, LocalDateTime, etc.) -->
            <!-- Required by JsonUtils.MAPPER to serialize UserSafeResponse.createdAt etc. -->
            <dependency>
                <groupId>com.fasterxml.jackson.datatype</groupId>
                <artifactId>jackson-datatype-jsr310</artifactId>
                <version>${jackson.version}</version>
            </dependency>

            <!-- YAML -->
            <dependency>
                <groupId>org.yaml</groupId>
                <artifactId>snakeyaml</artifactId>
                <version>${snakeyaml.version}</version>
            </dependency>

            <!-- Byte Buddy -->
            <dependency>
                <groupId>net.bytebuddy</groupId>
                <artifactId>byte-buddy</artifactId>
                <version>${byte-buddy.version}</version>
            </dependency>

            <!-- Netty -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-all</artifactId>
                <version>${netty.version}</version>
            </dependency>

            <!-- H2 -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>${h2.version}</version>
        </dependency>

        <!-- HikariCP -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
            <version>${hikaricp.version}</version>
        </dependency>

        <!-- PostgreSQL -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
        </dependency>

        <!-- MyBatis -->
        <dependency>
            <groupId>org.mybatis</groupId>
            <artifactId>mybatis</artifactId>
            <version>${mybatis.version}</version>
        </dependency>

        <!-- JWT -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>${jjwt.version}</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>${jjwt.version}</version>
            <scope>runtime</scope>
        </dependency>

            <!-- Password Hashing -->
            <dependency>
                <groupId>org.mindrot</groupId>
                <artifactId>jbcrypt</artifactId>
                <version>${jbcrypt.version}</version>
            </dependency>

            <!-- Security overrides (SCA remediation) -->
            <!-- Guava: CVE-2023-2976 (insecure temp dir) fixed in 32.0.1 -->
            <dependency>
                <groupId>com.google.guava</groupId>
                <artifactId>guava</artifactId>
                <version>${guava.version}</version>
            </dependency>
            <!-- Commons Text: CVE-2022-42889 (Text4Shell RCE) fixed in 1.10.0 -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-text</artifactId>
                <version>${commons-text.version}</version>
            </dependency>
            <!-- BouncyCastle (jdk15on) is DEFERRED: CVE-2024-34447 etc. are fixed in 1.78,
                 but 1.78 only ships the jdk18on coordinates (jdk15on was retired at 1.78).
                 Switching requires excluding jdk15on from pulsar-client / bouncy-castle-bc and
                 adding jdk18on:1.78. Since BouncyCastle is test-scope only (Pulsar test client)
                 and CVE-2024-34447 (BCJSSE hostname verification) is not exploitable in Baafoo's
                 usage, defer this to avoid risking the Pulsar system tests. Revisit with jdk18on
                 after validating Pulsar client startup against the system-test suite. -->
            <!-- snappy-java: CVE-2023-43642 (integer overflow) fixed in 1.1.10.3 -->
            <dependency>
                <groupId>org.xerial.snappy</groupId>
                <artifactId>snappy-java</artifactId>
                <version>${snappy-java.version}</version>
            </dependency>

            <!-- Spring / Tomcat patch overrides (SCA remediation, Java 8 lines only).
                 These MUST precede the spring-boot-dependencies BOM import so they win.
                 Spring: CVE-2024-38816 / CVE-2024-38819 fixed in 5.3.39.
                 Tomcat: CVE-2025-24813 fixed in 9.0.102. -->
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-core</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-context</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-beans</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-aop</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-expression</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-jcl</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-web</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-webmvc</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-tx</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-jdbc</artifactId>
                <version>${spring.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-test</artifactId>
                <version>${spring.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-core</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-el</artifactId>
                <version>${tomcat.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat.embed</groupId>
                <artifactId>tomcat-embed-websocket</artifactId>
                <version>${tomcat.version}</version>
            </dependency>

            <!-- Logging -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Test -->
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Testcontainers -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>kafka</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${testcontainers.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Mutation Testing -->
            <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>1.2.1</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-shade-plugin</artifactId>
                    <version>${maven-shade-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.pitest</groupId>
                    <artifactId>pitest-maven</artifactId>
                    <version>${pitest.version}</version>
                    <configuration>
                        <failWhenNoMutations>false</failWhenNoMutations>
                        <targetClasses>
                            <param>com.baafoo.core.util.*</param>
                            <param>com.baafoo.core.model.*</param>
                            <param>com.baafoo.server.handler.*</param>
                            <param>com.baafoo.server.auth.*</param>
                            <param>com.baafoo.server.storage.*</param>
                            <param>com.baafoo.agent.advice.*</param>
                            <param>com.baafoo.agent.plugin.*</param>
                        </targetClasses>
                        <targetTests>
                            <param>com.baafoo.*</param>
                        </targetTests>
                        <mutationThreshold>${pitest.mutationThreshold}</mutationThreshold>
                        <coverageThreshold>${pitest.coverageThreshold}</coverageThreshold>
                        <excludedMethods>
                            <param>toString</param>
                            <param>hashCode</param>
                            <param>equals</param>
                        </excludedMethods>
                        <excludedClasses>
                            <param>com.baafoo.core.config.*</param>
                        </excludedClasses>
                        <outputFormats>
                            <param>HTML</param>
                            <param>XML</param>
                        </outputFormats>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>**/entity/*Entity.class</exclude>
                        <exclude>**/mapper/*Mapper.class</exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
