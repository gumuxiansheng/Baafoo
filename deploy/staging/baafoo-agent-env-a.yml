# Baafoo Staging Agent Configuration — 环境A
# 用于 staging-a 环境的Agent配置

server:
  host: "server"
  useSsl: false
  apiPort: 8084
  httpPort: 9000
  tcpPort: 9001
  kafkaPort: 9002
  pulsarPort: 9003
  jmsPort: 9004
  apiKey: "staging-admin-key"

environment: "staging-a"

heartbeatIntervalSec: 30
pollIntervalSec: 10

consulEnabled: false
consulAddress: "localhost:8500"

protocols: []

maxRecordingSize: 10485760

rulesFilePath: ""
hotReload: true

failOpen: false

connectionRetries: 5
retryBackoffMs: 2000
