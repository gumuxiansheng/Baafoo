fix(security): P0 security hardening — bcrypt, XFF, admin lifecycle

- P0-1: replace SHA-256 password hashing with bcrypt (cost 12), with
  legacy SHA-256 verification + automatic rehash-on-login migration
- P0-2: remove hardcoded weak passwords (admin123/B@af00!Adm1n#2026)
  from source; ensureDefaultAdmin uses random strong password only on
  new install
- P0-3: flip localBypass default to false, add trustedProxies config
  with CIDR-aware X-Forwarded-For handling so spoofed XFF no longer
  bypasses auth
- P0-4: stop deleting+recreating admin on restart; role fix uses
  updateUserRole, hash migration deferred to next login
- P0-5: log warnings when PassthroughProxy SSL verification is disabled
- P0-6: verified RouteManager already uses atomic swap (no change needed)
- generateTempPassword now guarantees upper/lower/digit/special chars
  so it passes AuthService.validatePassword

Adds .workmemo/5_review/baafoo_architecture_improvement_todo.md as the
master plan tracking all P0/P1/P2/P3 items.
